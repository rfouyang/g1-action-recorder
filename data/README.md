# Runtime data

Pose definitions are stored under `poses/`. Generated MuJoCo preview images are
stored under `pose_previews/` and are ignored by Git.

Action source definitions are stored under `actions/definitions/`. Compiled NPZ
trajectories are stored under `actions/trajectories/`. Generated synchronized
MuJoCo action GIFs are stored under `action_previews/` and are ignored by Git.
