apiVersion: v1
kind: Secret
metadata:
  name: g1-3d-secrets
  labels:
    app.kubernetes.io/name: g1-3d
    app.kubernetes.io/component: authoring-console
type: Opaque
stringData:
  # Replace before applying, or create the secret out-of-band:
  #   kubectl create secret generic g1-3d \
  #     --from-literal=BYTEPLUS_API_KEY='...'
  BYTEPLUS_API_KEY: REPLACE_ME
