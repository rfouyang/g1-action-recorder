# Third-party source

Vendored source code is kept here with its upstream revision and license. The
Unitree SDK2 Python package will be added when read-only G1 recording is implemented.

Pink, Pinocchio, and the `quadprog` solver are installed as locked PyPI
dependencies rather than copied into this directory.
